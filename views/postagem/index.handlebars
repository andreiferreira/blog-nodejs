 
    <div class="card">
        <div class="card-body">
            <h3>{{postagem.titulo}}</h3>
            <hr>
            <small class="mt-4">Dada de publicação: {{#formatDate postagem.data}}{{/formatDate}}</small>
            <hr>
            <p class="mt-4">{{postagem.conteudo}}</p>
    </div>
    </div>
